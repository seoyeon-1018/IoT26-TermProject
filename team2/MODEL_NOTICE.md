# Model File Notice

Place your trained YOLO model file in the project root directory with this name:

best.pt

The main application uses:

MODEL_PATH = "best.pt"

If the file is too large for GitHub, upload it separately or use Git LFS.
